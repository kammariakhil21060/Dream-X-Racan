PORT=3001
APP_ORIGIN=http://localhost:3000
# Omit for persistent, embedded PostgreSQL development via PGlite.
DATABASE_URL=
DATA_DIR=.data/racan
PAYMENT_PROVIDER=development
# Required to seed initial accounts; never use demo credentials in production.
SEED_PASSWORD=
NODE_ENV=development
